{
  "name": "dcms-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "CI=false yarn workspace dcms-frontend build",
    "postinstall": "yarn build",
    "start": "yarn workspace dcms-backend start"
  }
}

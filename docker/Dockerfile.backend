FROM node:20-alpine AS base
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production

FROM node:20-alpine
WORKDIR /app
COPY --from=base /app/node_modules ./node_modules
COPY . .
RUN addgroup -S dcms && adduser -S dcms -G dcms
USER dcms
EXPOSE 3001
CMD ["node", "src/index.js"]
